{
  "name": "@aegis/geo-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript library for GeoHash computation, Pub/Sub payloads, and WebRTC signaling types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  }
}
